export interface Project {
  id: string;
  img: string;
  title: string;
  info: string;
  info2: string;
  url: string;
  repo: string;
}

export enum LOCALES {
  en = 'en',
  es = 'es',
  pt = 'pt',
}
