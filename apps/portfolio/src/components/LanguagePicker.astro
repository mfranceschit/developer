---
import { languages } from '@/i18n/ui';

interface Props {
  lang: string;
  currentPath: string;
}

const { lang, currentPath } = Astro.props;
---

<div class="flex gap-2.5 text-xs">
  {
    Object.entries(languages).map(([code, label]) => (
      <a
        href={`/${code}${currentPath}`}
        aria-current={lang === code ? 'page' : undefined}
        class={`transition-colors duration-[120ms] font-sans ${
          lang === code
            ? 'font-semibold text-bellwether-blue'
            : 'font-normal text-[var(--mf-gray-400)] hover:text-bellwether-blue'
        }`}
      >
        {label.slice(0, 2).toUpperCase()}
      </a>
    ))
  }
</div>
